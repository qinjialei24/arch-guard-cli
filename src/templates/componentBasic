import React from "react";
import './{{fileName}}.less'

interface {{componentName}}Props {
  children?: React.ReactNode;
}

export const {{componentName}} = (props: {{fileName}}Props) => {
  return (
    <div className="Ba{{fileName}}">
      {props.children}
    </div>
  );
};

Ba{{fileName}}.defaultProps = {};
